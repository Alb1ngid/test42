1 раз
git init - инициализация гита
git remote add origin ссылка на ваш репозиторий - связывает ваш проект и ваш гит перозиторий

1 раз
create .gitignore
        venv
        idea...

регулярно:
git add .\namefile-добавляет в гит ваши файлы
git commit -m 'имя коммита'-сохраняет ваш результат
git push origin master\main


полезные команды:
git rm --cached namefile- удаляет файлы с репозитория
git rm --cached -r namedir- удаляет папки с файлами из репозитория
git clone : ссылка на проект - полностью скачивает проект
git remote remove origin : отвязывает от гита весть проект


pip install
pip freeze
pip freeze > name.txt
pip list
pip install -r name.txt
python -m venv nameenv
nameenv\Scripts\activate